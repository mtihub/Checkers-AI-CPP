# Checkers-AI-CPP
A project to make a game of checkers that includes an AI. The project has an highly object oriented design. The AI uses Minimax algorithm with a maximum depth count. No GUI libraries involved, everything is done in the command prompt.
